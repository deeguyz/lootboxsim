import SignOutButton from '../../components/auth/SignOutButton';

const Dashboard = () => {
  return (
    <div>
      <div>Dashboard</div>
      <SignOutButton />
    </div>
  );
};

export default Dashboard;
